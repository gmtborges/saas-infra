# vim:ft=yaml.ansible
---
- name: Install Nginx with certs as reverse-proxy in a debian machine
  hosts: saas
  become: true
  vars:
    domain_name: example.com
    email: webmaster@example.com

  roles:
    - nginx
